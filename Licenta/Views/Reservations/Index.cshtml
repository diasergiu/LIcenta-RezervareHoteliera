@model IEnumerable<Licenta.Entityes.Reservations>
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers

@{
    ViewData["Title"] = "Index";
	Layout = "_Layout";
}

<h2>You'r Reservations</h2>

<p>

</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CheckIn)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CheckOut)
            </th>
            <th>
				Hotel
                <!--@Html.DisplayNameFor(model => model.IdRoomNavigation.IdHotelNavigation)-->
            </th>
            <th>
				RoomNumber
                <!--@Html.DisplayNameFor(model => model.IdRoomNavigation)-->
            </th>
			<th>
				region
			</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.CheckIn)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.CheckOut)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.IdRoomNavigation.IdHotelNavigation.HotelName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.IdRoomNavigation.RoomNumber)
                </td>
				<td>
					@Html.DisplayFor(modelItem => item.IdRoomNavigation.IdHotelNavigation.IdLocationNavigation.RegionName)
				</td>
                <td>
                    <!--<a asp-action="Edit" asp-route-id="@item.IdReservations">Edit</a> -->
                    <a asp-action="Details" asp-route-id="@item.IdReservations">Details</a> |
                    <a asp-action="Delete" asp-route-id="@item.IdReservations">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
